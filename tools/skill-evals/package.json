{
  "name": "skill-evals",
  "private": true,
  "description": "npm surface for skillcheck, the skills lint/eval tool (uinaf/skillcheck)",
  "scripts": {
    "lint": "skillcheck lint --root ../..",
    "run": "skillcheck run --root ../..",
    "sweep": "skillcheck sweep --root ../..",
    "summarize": "skillcheck summarize --root ../.."
  },
  "devDependencies": {
    "skillcheck": "github:uinaf/skillcheck#v0.1.3"
  }
}
