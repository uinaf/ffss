{
  "name": "csv-to-json-cli",
  "version": "2.1.0",
  "description": "Convert CSV files to JSON",
  "bin": {
    "csv2json": "./dist/cli.js"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc",
    "typecheck": "tsc --noEmit -p tsconfig.test.json",
    "lint": "eslint --ext .ts src/",
    "deadcode": "knip",
    "test": "jest",
    "verify": "npm run clean && npm run typecheck && npm run lint && npm run deadcode && npm run build && npm test -- --ci",
    "hooks:install": "git rev-parse --git-dir > /dev/null 2>&1 && chmod +x .git-hooks/pre-push && git config core.hooksPath .git-hooks || echo 'not a git checkout; skipping hook install'",
    "prepare": "npm run hooks:install"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.0.0",
    "eslint": "^8.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "@types/jest": "^29.5.0",
    "@types/node": "^22.0.0",
    "knip": "5.0.0"
  }
}
