# Local-only development configuration (never committed).
# Managed Codex/Claude worktrees receive .env.local via .worktreeinclude.
.env.local

# Runtime artifacts owned by tools/dev_service.py: process state and service log.
.dev/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
