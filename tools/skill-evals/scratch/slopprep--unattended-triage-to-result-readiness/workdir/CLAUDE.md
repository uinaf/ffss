The canonical agent guide for this repository is [`AGENTS.md`](AGENTS.md). Read it
instead of this file; no guidance is maintained here.

This is a pointer file only because the checkout was prepared in an environment
that could not create symlinks. Replace it with the canonical form when you can:

```bash
rm CLAUDE.md && ln -s AGENTS.md CLAUDE.md
```
