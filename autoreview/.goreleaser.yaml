version: 2

project_name: autoreview

builds:
  - id: autoreview
    main: ./cmd/autoreview
    binary: autoreview
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X github.com/uinaf/autoreview/internal/buildinfo.version={{ if .IsSnapshot }}v{{ .Version }}{{ else }}{{ .Tag }}{{ end }}
      - -X github.com/uinaf/autoreview/internal/buildinfo.commit={{ .FullCommit }}
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64

archives:
  - id: autoreview
    name_template: "{{ .ProjectName }}_{{ .Tag }}_{{ .Os }}_{{ .Arch }}"
    formats: [tar.gz]
    files:
      - LICENSE
      - README.md

checksum:
  name_template: checksums.txt
  algorithm: sha256

notarize:
  macos:
    - enabled: '{{ isEnvSet "APPLE_DEVELOPER_ID_CERTIFICATE_P12_BASE64" }}'
      ids:
        - autoreview
      sign:
        certificate: "{{ .Env.APPLE_DEVELOPER_ID_CERTIFICATE_P12_BASE64 }}"
        password: "{{ .Env.APPLE_DEVELOPER_ID_CERTIFICATE_PASSWORD }}"
      notarize:
        issuer_id: "{{ .Env.APPLE_NOTARY_API_ISSUER_ID }}"
        key_id: "{{ .Env.APPLE_NOTARY_API_KEY_ID }}"
        key: "{{ .Env.APPLE_NOTARY_API_KEY_P8_BASE64 }}"
        wait: true
        timeout: 20m

signs:
  - cmd: cosign
    signature: "${artifact}.sigstore.json"
    args:
      - sign-blob
      - "--bundle=${signature}"
      - "${artifact}"
      - --yes
    artifacts: checksum

snapshot:
  version_template: "{{ incpatch .Version }}-snapshot-{{ .ShortCommit }}"

changelog:
  disable: true

release:
  github:
    owner: uinaf
    name: autoreview
  mode: append
  replace_existing_artifacts: true
  prerelease: auto

homebrew_casks:
  - name: autoreview
    ids:
      - autoreview
    binaries:
      - autoreview
    repository:
      owner: uinaf
      name: homebrew-tap
      branch: main
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Casks
    commit_author:
      name: "{{ .Env.RELEASE_BOT_NAME }}"
      email: "{{ .Env.RELEASE_BOT_EMAIL }}"
    commit_msg_template: "chore(autoreview): update to {{ .Tag }}"
    homepage: https://github.com/uinaf/autoreview
    description: Structured independent code review CLI
    license: MIT
    dependencies:
      - formula: git
      - formula: trufflehog
    url:
      verified: github.com/uinaf/autoreview/
