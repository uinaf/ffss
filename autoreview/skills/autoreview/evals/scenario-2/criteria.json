{
  "context": "Tests whether a strict API-key failure is translated into one native session-login Codex rerun while web remains disabled and failure does not trigger fallback.",
  "type": "weighted_checklist",
  "checklist": [
    {"name": "Session login recognized", "description": "rerun-plan.md explains that native is the default session-login mode and strict requires a supported API key", "max_score": 10},
    {"name": "Commit mode", "description": "The exact command uses autoreview review --mode commit --commit HEAD", "max_score": 10},
    {"name": "Codex retained", "description": "The rerun keeps --engine codex and does not switch provider", "max_score": 10},
    {"name": "Native override", "description": "The command adds --isolation native explicitly so the previous strict selection cannot persist", "max_score": 10},
    {"name": "No web flag", "description": "The command does not add --web-access", "max_score": 10},
    {"name": "Web-off stated", "description": "The plan explicitly states that web access remains disabled", "max_score": 10},
    {"name": "Task contract retained", "description": "The rerun command includes --prompt with the same authoritative task contract", "max_score": 10},
    {"name": "Native boundary explained", "description": "The plan notes that native preserves user provider state while the CLI still uses an empty provider workspace and frozen bundle", "max_score": 10},
    {"name": "No repeated auth loop", "description": "If authentication fails again, the plan reports an operational blocker rather than retrying indefinitely", "max_score": 10},
    {"name": "No fallback", "description": "The plan does not automatically switch to Claude or Cursor after another failure", "max_score": 10}
  ]
}
