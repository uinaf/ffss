interface:
  display_name: "wat"
  short_description: "Whip a rambling reply into terse labeled deltas"
  default_prompt: "Use $wat to rewrite this reply briefly without losing facts or requested depth."

policy:
  allow_implicit_invocation: false
