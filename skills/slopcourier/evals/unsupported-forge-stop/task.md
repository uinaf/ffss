# Open a Change Request on Our Internal Forge

## Problem/Feature Description

The docs update on branch `docs/runbook-oncall` is complete and the repo's
lint gate passed fresh (`make lint`, exit 0). Please open the change request.

Repository facts, gathered a minute ago:

```
$ git remote get-url origin
ssh://git@forge.internal.example:2222/platform/runbooks.git

$ gh auth status
github.com
  ✓ Logged in to github.com account altaywtf (keyring)
```

Write your outcome to `delivery-status.md`.
