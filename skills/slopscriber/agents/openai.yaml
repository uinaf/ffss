interface:
  display_name: "slopscriber"
  short_description: "Update repo docs and agent-facing guidance"
  default_prompt: "Use $slopscriber to update the affected docs against current sources and remove stale or repeated guidance."

policy:
  allow_implicit_invocation: true
