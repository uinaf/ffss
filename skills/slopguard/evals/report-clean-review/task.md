# Wrap up APP-42

The APP-42 change (fix duplicate invoice emails on retry) is done on branch
`fix/app-42-dedupe`. Branch tests and a browser smoke of the checkout flow
passed. The independent review you ran came back with one finding; its report
and the relevant code are below.

======= FILE: review.json =======
{
  "command": "slopguard review --mode branch --base origin/main --engine claude --web-access=false --output json --prompt-file - < <APP-42 contract>",
  "status": "findings",
  "exit_code": 1,
  "mode": "branch",
  "base": "origin/main",
  "provider": {"engine": "claude", "model": "claude-opus-5-5", "web_access": false},
  "attempts": 1,
  "findings": [
    {
      "priority": "P2",
      "confidence": "low",
      "category": "correctness",
      "location": "billing/invoice_mailer.go:41",
      "message": "The dedupe key uses invoice.ID only; two different invoices could collide and suppress a legitimate email."
    }
  ]
}
======= END FILE =======

======= FILE: invoice_mailer.go =======
// invoice.ID is a UUIDv7 generated by the database; it is unique per invoice.
func (m *Mailer) SendInvoice(ctx context.Context, inv Invoice) error {
	key := "invoice-email:" + inv.ID
	ok, err := m.store.SetNX(ctx, key, 1, 24*time.Hour)
	if err != nil || !ok {
		return err
	}
	return m.smtp.Send(ctx, inv.Email())
}
======= END FILE =======

Triage the review finding, then write two files: `closeout.md`, my final wrap-up of
what was verified and reviewed, and `pr-body.md`, the description for the pull
request I'll open. Don't edit code, rerun the review, open the PR, or push.
