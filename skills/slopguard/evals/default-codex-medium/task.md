# Choose the default review provider

A completed local change has passed its builder checks. The user asks for an
independent review without naming a provider, model, or reasoning effort, and
trusted configuration does not select any of them. Codex is installed.

Write `response.md` with the provider and effort choice plus the exact review
command shape. Include how an explicit user or trusted-configuration choice
would affect that default.
