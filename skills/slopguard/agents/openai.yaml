interface:
  display_name: "slopguard"
  short_description: "Close out code changes with one independent review"
  default_prompt: "Use $slopguard to review this completed change against its acceptance criteria and iterate until clean."

policy:
  allow_implicit_invocation: false
