interface:
  display_name: "slopguard"
  short_description: "Close out code changes with one independent review"
  default_prompt: "Use $slopguard for independent review of this completed change against its acceptance criteria."

policy:
  allow_implicit_invocation: true
