interface:
  display_name: "slopspec"
  short_description: "Save agreed work in the repository tracker"
  default_prompt: "Use $slopspec to save the agreed work as the smallest resumable plan in the repository's tracker."

policy:
  allow_implicit_invocation: false
