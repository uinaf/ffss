{
  "context": "Tests whether the agent adds machine-readable health and request signals, implements collision-free worktree lifecycle in a structured Python tool with bounded readiness and cleanup, and handles the explicitly required ignored local config without prescribing one allocation algorithm.",
  "type": "weighted_checklist",
  "checklist": [
    {
      "name": "Health endpoint exists",
      "max_score": 6,
      "description": "app.py defines a GET /health route"
    },
    {
      "name": "Health response has status field",
      "max_score": 6,
      "description": "The /health endpoint returns a JSON object containing a 'status' key (e.g., {\"status\": \"ok\", ...})"
    },
    {
      "name": "Health response is operationally useful",
      "max_score": 5,
      "description": "The health response includes enough stable machine-readable context to distinguish ready from unavailable; version, uptime, dependency readiness, or an equivalent useful signal is accepted"
    },
    {
      "name": "Structured JSON request logging",
      "max_score": 8,
      "description": "app.py emits a structured JSON log line for each request (not plain print statements) — containing at minimum method, path, and status fields"
    },
    {
      "name": "Concurrent port allocation is collision-free",
      "max_score": 8,
      "description": "tools/dev_service.py obtains or computes an isolated port per worktree or task using a runner assignment, deterministic allocation, ephemeral-port protocol, or equivalent collision-resistant mechanism"
    },
    {
      "name": "No hardcoded port 5000",
      "max_score": 5,
      "description": "tools/dev_service.py does not use port 5000 or another fixed application port as the worktree allocation"
    },
    {
      "name": "Python lifecycle starts the app",
      "max_score": 6,
      "description": "tools/dev_service.py starts the Flask application through a Python subprocess API and retains enough state to manage it"
    },
    {
      "name": "Bounded readiness wait",
      "max_score": 7,
      "description": "tools/dev_service.py waits for the health endpoint until a finite wall-clock deadline before declaring success"
    },
    {
      "name": "Non-zero exit on boot failure",
      "max_score": 7,
      "description": "tools/dev_service.py exits non-zero and cleans up its child if the app fails to start within the readiness deadline"
    },
    {
      "name": "Structured lifecycle state",
      "max_score": 5,
      "description": "tools/dev_service.py persists structured state containing at least the owned PID and allocated port, and validates that state before stopping a process"
    },
    {
      "name": "Stop command cleans up owned service",
      "max_score": 6,
      "description": "tools/dev_service.py stop terminates only the service identified by the current worktree's validated lifecycle state and removes stale runtime state"
    },
    {
      "name": "Port collision rationale documented",
      "max_score": 5,
      "description": "observability-notes.md explains how the chosen resource-allocation contract prevents collisions between concurrent worktrees and who owns that allocation"
    },
    {
      "name": ".worktreeinclude exists",
      "max_score": 5,
      "description": "The solution creates a .worktreeinclude file at the repository root"
    },
    {
      "name": ".worktreeinclude is narrow",
      "max_score": 5,
      "description": ".worktreeinclude includes .env.local or the required local config path, and does not use broad patterns such as *, .env*, secrets/, node_modules/, or build/cache directories"
    },
    {
      "name": ".gitignore matches worktree include",
      "max_score": 4,
      "description": ".gitignore ignores the same local config file that .worktreeinclude includes, so the file is eligible for managed Codex or Claude worktree copying"
    },
    {
      "name": "Worktree include rationale documented",
      "max_score": 4,
      "description": "observability-notes.md explains that managed Codex or Claude worktrees copy only matching gitignored files from .worktreeinclude, and that manual worktrees/hooks need their own copy path"
    },
    {
      "name": "Dev environment observability",
      "max_score": 8,
      "description": "observability-notes.md or app.py adds observability to the development/local environment (not just describing it as a production concern)"
    }
  ]
}
