# Prepare Without Starting

## Problem/Feature Description

I want to use slopmachine for the plan below eventually. For now, assess the
plan and prepare the execution outline in `execution-plan.md`. Do not edit
source, run checks, install tools, open a PR, or start implementing yet.

Plan: add a `--json` flag to export with integration coverage, then document
the actual output contract. When I authorize execution, use slopguard review
and deliver a held PR. Keep the CSV output unchanged.

The owning code and existing integration suite have already been identified.
The repository supports this plan and requires feature PRs. No acceptance
detail is missing. This exercise provides no source repository.
