# Run the Agreed Plan with Checkpoints

## Problem/Feature Description

We agreed on the plan below. Execute it as a governed, checkpointed run.

Plan: add a `--json` flag to the export command, then update the CLI
reference to document it. Two units, docs blocked on the flag landing.

Write your outcome and anything you need from me to `run-status.md`.
