interface:
  display_name: "slopmachine"
  short_description: "Carry an agreed plan through implementation and delivery"
  default_prompt: "Use $slopmachine to complete the agreed plan through implementation, verification, review, and delivery, honoring the requested hold or merge endpoint."

policy:
  allow_implicit_invocation: true
