interface:
  display_name: "slopmachine"
  short_description: "Run a plan with authorization and evidence-backed loops"
  default_prompt: "Use $slopmachine to execute the agreed plan through its required verification, review, and delivery gates."

policy:
  allow_implicit_invocation: true
