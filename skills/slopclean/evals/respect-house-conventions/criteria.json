{
  "context": "Tests the boundary that repository conventions beat the skill's own rules: the mandated JSDoc block on the exported function must survive even though it resembles doc slop, while inline narration and change-history comments still get cut.",
  "type": "weighted_checklist",
  "checklist": [
    {
      "name": "JSDoc preserved",
      "max_score": 30,
      "description": "The exported createRateLimiter keeps a JSDoc block with summary, @param tags, and @returns, per CONTRIBUTING.md"
    },
    {
      "name": "Inline narration deleted",
      "max_score": 24,
      "description": "The line-by-line body comments ('Get the current time', 'Consume one token', 'Update the last refill timestamp', etc.) are removed"
    },
    {
      "name": "History comment deleted",
      "max_score": 12,
      "description": "The 'refactored in the latest iteration' note is removed"
    },
    {
      "name": "Behavior unchanged",
      "max_score": 20,
      "description": "The token-bucket logic (capacity clamp, time-based refill, single-token take, boolean return) is untouched"
    },
    {
      "name": "Convention acknowledged",
      "max_score": 8,
      "description": "The response signals it kept the JSDoc because the repository requires it, rather than by accident"
    },
    {
      "name": "Style guide untouched",
      "max_score": 6,
      "description": "CONTRIBUTING.md is not modified"
    }
  ]
}
